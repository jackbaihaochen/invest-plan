{
  "name": "invest-plan",
  "private": true,
  "description": "1億円投資ダッシュボード (Google Apps Script + Sheets)",
  "scripts": {
    "test": "node tests/parse.test.js && node tests/projection.test.js",
    "push": "clasp push"
  }
}
