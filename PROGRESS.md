# PROGRESS

**Current Status**：`claude/rebuild` 分支。**第 1 阶段（本地可用的看板）完成**，54 项测试
（本物の CSV が手元に無ければ 51 + 3 skip）。**仓库里不再有真实交易数据**。
`npm run dev` 打开，拖入 CSV 即可使用：今月の環、成長グラフ（元本の線＋実測の点／1億までの見通し）、
到达日与落后多久、必要な毎月の投入額、実績年率、记录一笔看到「提前 N 天」、
资产分类、月次柱状、持仓、交易一览。配色为亮色单一主题。数据存 localStorage。

顺序已按用户同意调整为「先界面后后端」：先做出能看能改的东西，
需要手工配置的基础设施（表格 / Apps Script / 邮件触发器）留到最后。

下一步：push 到 main → 用户改 Pages 设置 → 第 3 阶段 Sheet + Apps Script + 基準価額メール。
仓库保持公开：配信物里没有任何交易数据，测试夹具也是作り物。

**Blockers**：无（CSV 已拿到并验证，见 proposal §8）。等提案批准即可开工。

---

## Plan

- [x] 架构提案获批
- [x] 项目骨架（Vite + React + TS + vitest）
- [x] 第 1 阶段：本地可用的看板（localStorage，无后端）
- [x] 第 2 阶段：GitHub Pages 部署（要用户在 Settings → Pages 把 Source 改成 GitHub Actions）
- [x] CSV 解析（持仓快照 + 交易履历，19 项测试对真实文件通过）
- [x] 手动记录与 CSV 的对账（coverageEnd 接缝 + 匹配规则）
- [x] 交易一览（可按 入金/出金/口座内/収益 筛选）+ 分类饼图
- [ ] 各分类の取得原価の推移（銘柄ごとの平均取得原価で戻す必要あり）
- [ ] Google Sheet 读路径 + 数据模型
- [ ] Apps Script 写端点（令牌鉴权）
- [ ] Apps Script 日次触发器：基準価額メール → prices 表
- [x] 投入记录与月度进度环
- [x] 成長グラフ（累计投入の線 + 実測値の点 + 1億までの見通し）
- [x] 逆算「この年に届かせるには毎月いくら」＋ 実績年率（金額加重）
- [ ] 看板其余部分（里程碑、What-if）

---

## Completed

### 2026-09-02 · GitHub Pages の配信を用意（第 2 阶段）
`.github/workflows/deploy.yml` —— `tsc -b` → `npm test` → `build` が通ったものだけ配る。
本番ビルドを `/invest-plan/` サブパスで実機確認：作り物 CSV を入れて全カードが描画、
console エラーなし、`valuePoints` も記録される。手順は [docs/deploy.md](docs/deploy.md)。

ついでに `index.html` の `color-scheme` が `light dark` のままだったのを `light` に直した。
亮色一本にしたのに、ブラウザ側だけ暗いフォーム部品とスクロールバーを描いていた。

**残る制約**：データは端末ごとの localStorage。携帯で開いても空で、その端末で
もう一度 CSV を入れる必要がある。多端末は第 3 阶段まで解けない。

### 2026-09-02 · テスト用 CSV を作り物に差し替え、本物をリポジトリから外す
きっかけはユーザーの指摘 ——「テストデータなら本物である必要はない」。そのとおりで、
**アプリは実行時にあの3ファイルを一度も読まない**。読むのはテストだけ。

- `tests/fixtures/make-rakuten.mjs`（生成器）→ `tests/fixtures/rakuten/` に CP932 で出力。
  合計段は行から**計算して**出すので、`Σ評価損益 = 合計段` が二度打ちの照合ではなく
  本当の検算のまま残る。明細と合計段の 6 円差は意図的に残した（米株行の丸めを説明する
  テストが1本あるため）。
- `tests/fixtures/rakuten-private/`（`.gitignore`）に本物を置くと `real-data.test.ts` が
  追加で走る。**期待値を書いていない** —— ファイル自身が名乗る合計と突き合わせるだけ。
  置かなければ 3 件 skip。だから **CI の緑は手元の緑より弱い**。
- 散文の金額も落とした（`proposal.md` の対账表、`PROGRESS.md`、`NOTES-background.md`）。
  残したのは主張のほう：「Σ評価損益 は記載と完全一致」「差 −6 円は米株行の丸め」。

作り物と本物の**両方**で 54 項/51 項が通ることを確認済み。

### 2026-09-01 · 成長グラフと亮色配色，实机发现并修掉两处 UI bug
`src/domain/growth.ts` + `src/ui/Growth.tsx` + `src/store.ts` 的 `valuePoints`，51 项测试。

**曲线只画拿得出证据的东西**：累计投入の線（取引履歴から厳密）、実測総資産の点
（保有商品 CSV を取り込むたびに1点、今は1点だけ）、これからの2本（実績ペース / 計画）。
過去の評価額は復元できないので描かない —— 見 `docs/decisions.md`。

新しい数字：実績年率（金額加重、期間つき）、本金と収益の内訳、
「この年に届かせるには毎月いくら」の逆算。年率は設定（0/3/5/7/10%）に出した
—— 実績と既定 7% の差を黙って抱えない。金額そのものは画面で見る。

实机（真实 CSV，浏览器）发现并修掉两处，两处 tsc/vitest 都抓不到：
- **CSV を3つまとめて落とすと最後の1つしか残らなかった** —— `onImport` が閉じ込めた
  `store` を使っていて、同じ tick の3回の更新が互いを潰していた。ドロップ枠には
  「まとめて放り込んで大丈夫」と書いてある、つまり**普通の使い方が壊れていた**。
  関数形の更新に直した。
- **月次の棒グラフが1本も描かれていなかった** —— 縦並び flex で `align-self` は
  幅の指定になるので、空の div の幅が 0 になっていた。ついでに 0 を挟んで
  上下に伸びる形に直し、正負が同じ「円/px」で読めるようにした。

### 2026-09-01 · 第 1 阶段完成：本地可用的看板
`src/domain/{projection,contribution,valuation,model}.ts` + `src/ui/*` + 38 项测试。
在浏览器里用真实 CSV 实机确认：総資産・累计投入・到达年月・落后多久都算得出来，
记入 5 万显示「提前 N 天」（与钉住的测试一致）。具体金额不写进仓库。

**接缝生效**：记入 5 万后环前进，并标注「其中 5 万是你自己记的，等 CSV 确认」。
测试里有一条专门守着不重复计数 —— 同一笔在 CSV 出现后，手动记录退位而不是相加。

实机发现并修掉两处：
- **ファイル名から日付が読めないとき「今日」に倒していた** —— 会把几个月前的評価額
  显示成当日值。改成 null 并在界面上说明读不到。这类错误正是这个工具最不该犯的。
- **見通しと環に同じ数（中央値）を使っていた** —— 问的不是同一件事。见通し用近 6 个月
  **平均**（实际积累速度），环用**中央值**（大多数月份打得赢的目标）。

验证用的 dev 夹具已删除。要再复现：`cp tests/fixtures/rakuten/*.csv public/_dev/` 后
在页面控制台 fetch 进 localStorage，或者直接把 CSV 拖进页面。

### 2026-09-01 · CSV 解析器完成，对真实文件全部验证通过
`src/domain/{csv,types,holdings,transactions}.ts` + 19 项测试。测试直接读仓库里的
原始 CP932 文件，不放转码副本 —— 能否用 `TextDecoder('shift_jis')` 读也是被测对象。

断言的是硬数字，不留丸め誤差的逃生口：Σ評価損益 与記載**完全一致**、
Σ時価 ＋ 預り金 与 資産合計 差 −6（美股行四舍五入）、NISA 取得原価、
純入金、coverageEnd 全部钉死。

未知の取引区分は**抛错而不是默默归类** —— 默认归 internal 会漏掉入金，
默认归 inflow 会重复计。两种都是静默出错。

写测试时发现三处我自己写错的断言（跨币种振替当天 JP 侧还有一笔金積立、
income 是 61 笔不是 60、JP/US 不重复这一点只对売買成立不对振替成立），
已按数据实际的样子修正。合计段的标签是「預り金合計」不是「預り金等合計」。

### 2026-09-01 · 拿到真实 CSV，三个文件全部解析验证通过
`assetbalance(all)` / `adjusthistory(JP)` / `adjusthistory(US)`，均为 CP932。
重算 Σ時価 ＋ 預り金 与記載 資産合計 差 −6 円（美股行四舍五入）；
Σ評価損益 与記載**完全一致**；NISA 枠使用与上一版记录一致。

三个会静默算错的坑已记录进 proposal §8：明细表头 `［単位］` 重复 4 次（不能按名建索引）、
取得原価必须由「時価 − 評価損益」反推（各種別倍率不同）、`振替出金` 是金の定額積立
（内部移动，不是取出）。US 文件第 3/4 列顺序与 JP 相反。

全期间净入金与现资产的差额 = 运用益，三者互相对得上。

### 2026-09-01 · 重启项目，确定架构方向
分支 `claude/rebuild` 从 `main`（仅一行 README 的 initial commit）切出，
只带过来 `CLAUDE.md` 和 `NOTES-background.md`。上一版的实现全部留在
`claude/savings-goal-tracker-0dv6g6`，不再使用。

实测确认 Google Sheet 的 `gviz/tq?tqx=out:csv` 端点带 CORS 头，静态页面可直接读取，
读路径零成本。写路径定为用户一次性部署的 Apps Script Web App。

激励机制围绕「手动买入补足 40 万/月差额」设计，而非每日打卡 —— 見 `docs/decisions.md`。
