# 1億円プロジェクト — 背景知识（上一版实现的经验教训）

> 这里记录的是分支 `claude/savings-goal-tracker-0dv6g6` 留下的**认知**，不是当前的设计。
> 那一版的实现被判定为「不够好」并整体废弃，但下面这些事实是花代价换来的，重做时不必再撞一遍。

---

## 1. 项目要解决什么

一个人的资产追踪看板，目标是**攒到 1 亿日元**。它要回答的核心问题不是「我有多少钱」，
而是：

- 按现在的实际节奏，我什么时候到 1 亿？**比计划落后了多久？**
- 这个月投了多少？够不够 40 万的目标？

资产分散在 4 个地方：楽天証券（大头）、野村證券持株会、bitFlyer、ゆうちょ銀行。

**用户明确认可的表达方式**（值得沿用）：
- 首屏是「**落后 X 年 Y 个月**」，不是「进度 13%」的进度条。
- 说明书里写清楚 7% 是美元名目收益、特定口座要交 20.315% 税、持株会 = 工资和股票押同一家公司。
- 界面文案用中文，专有名词保留日文。

---

## 2. 为什么这一版被放弃

commit `cb740c0` 里那句话是整段历史里最有用的一句：

> "The Gmail-driven dashboards were reporting numbers the user did not trust and read as a
> wall of figures."

即：**Gmail 解析出来的数字用户不信任，而且页面是一堵数字墙**。之后才退回到「用户自己填的
Google 表格」（Track 0）。三条路线依次是：

| | 做法 | 结局 |
|---|---|---|
| Track 2 | Apps Script + Gmail 定时抓取 + 周报月报 | 代码还在，但需要用户自己做 OAuth 授权和 Web 应用部署 |
| Track 1 | Artifact 页面 + claude.ai 连接器读 Gmail/Drive | 只在 claude.ai viewer 里能刷新，直接开链接是只读 |
| Track 0 | `tools/build-sheet.py` 生成 .xlsx，用户导入 Google 表格 | 当前推荐，也是当前分支的落点 |

功能清单不用我复述，`README.md` 写得很全，而且它在 `main` 上会一直在。

---

## 3. 平台硬约束（每一条都是实测撞出来的）

这部分是重做时最省时间的内容。

1. **通过 Drive 连接器建表会丢掉所有公式** —— 实测连 `=1+1` 都存不进去，CSV 和 xlsx 两条
   转换路径都验过。这一条直接毙掉了两版设计，也是为什么最后必须导出 .xlsx 让用户手动导入。
2. **Artifact 的连接器能力只在被 claude.ai viewer framed 时生效**。直接访问 artifact URL，
   `use()` 一律返回 `null`；另存为本地文件连 `window.claude` 都没有。
3. **Artifact 的 CSP 禁止 fetch** —— 页面拿不到任何行情，只能绕道「Google 表格里写
   GOOGLEFINANCE，页面通过 Drive 读结果」。
4. **Apps Script 有两步只能用户在浏览器里做**：授予 OAuth 同意、部署 Web 应用。Agent 做不了。
5. **`GmailApp` 只认 `https://mail.google.com/` 这一个 scope**，没有更细的只读版本。

---

## 4. 数据源覆盖率（设计反复变的真正原因）

**按金额算，能自动化的部分最多只有 18〜24%。** 这是重做时第一个要接受的事实。

| 资产 | 占比 | 自动化情况 |
|---|---:|---|
| 投資信託 | 69% | **GOOGLEFINANCE 零覆盖**，只能靠邮件里的基準価額或 CSV |
| 金现物 | 13% | 同样零覆盖，手填每克日元 |
| 美股 / ETF / 汇率 | 18% | `GOOGLEFINANCE("NASDAQ:NVDA")` / `CURRENCY:USDJPY` 可用 |
| BTC | 5.6% | `CURRENCY:BTCJPY` 实测可用，**不需要 bitFlyer API key** |
| 日本股 `TYO:` | 1.2% | 实测返回空，必须有退回手填价的路径 |

其他事实：

- **楽天証券没有个人可用的公开 API**，但每个工作日发「投信基準価額メール」、每次成交发
  「約定メール」。解析这两类邮件足以重建投信持仓，不用爬虫、不违反利用規約。
  （历史邮件量：约 201 封基準価額（2025/10 起）+ 18 封約定（2025/06 起）。）
- **楽天証券的 CSV 导出 `assetbalanceall`（保有商品詳細，CP932 编码）是权威数据源**：
  数量、取得原価、账户、当日价格都在里面。注意**預り金在合计栏而不是明细栏**，要按标签单独取。
  另一个导出 `adjusthistoryJP` 的入出金行可以还原每月净入金。
- **野村持株会和ゆうちょ完全没有自动化路径**：ゆうちょ的参照系 API 只对
  電子決済等代行業者开放；入金通知邮件不含金额。持株会只有季度残高明細書。这两个只能手填 +
  超期标红。
- 約定メール只有购入金额、没有口数，口数是用当日基準価額反推的，有微小误差 —— 所以才需要
  CSV 来校准。

---

## 5. 已经踩过的坑（别再踩）

- **`LOOKUP(2,1/(range<>""),range)` 在 Google 表格里返回的是第一行，不是最后一行。**
  实机症状：累计本金显示 9,552,231（最老的那个月），应为 10,387,818。
  改用 `INDEX(range, MATCH(MAX(年月列), 年月列, 0))`。
- **`lineCells_` 丢弃空单元格会让定位表错列** —— `| ゆうちょ_残高 |  | 円 |` 会把「円」读成金额。
  按位置解析时必须保留空位（`splitCells_`）。
- 邮件名称匹配要做 **NFKC 归一化**：`eMAXIS　Slim　米国株式（Ｓ＆Ｐ５００）` 要能对上
  `eMAXIS Slim 米国株式(S&P500)`。
- 一封基準価額邮件里可能有多支基金，金额和口座区分各不相同。
- 跨年成交（12 月下单、1 月成交）。
- 同一封邮件重复处理不能重复记账（幂等）。
- xlsx 过一遍 Google 表格导入器时，`SPARKLINE` / `ARRAYFORMULA` 这类 Google 专有函数会碎。
  只用 Excel 和 Sheets 共有的函数；进度条用 `REPT("█",n)` 画。`tools/check-sheet.py` 会机械检查。

---

## 6. 对账锚点（新实现拿这些自检）

以 **2026/08/29** 的楽天証券「保有商品詳細」快照为准：

- **26 个持仓，按数量×价格重算合计 *** 円**，与楽天的資産合計一致，逐行误差 ≤1 円。
- **NISA 生涯投资枠用量要按取得価額累加，不是市值**：*** / 18,000,000。
  按市值算会高估约三分之一。
- 起始累计本金 **9,301,761**（楽天取得原価 9,494,115 + BTC 893,703 − 已记入近 12 个月的部分），
  还没算野村/ゆうちょ的本金。
- USD/JPY 快照值 **160.06**（GOOGLEFINANCE 取不到时的 fallback）。
- BTC 数量 **0.061912**（来自用户自己的「Bitcoin trade history」表，最后一笔 2026/01/29），
  成本 893,703。
- 近 12 个月实际节奏 **90,505 円/月**（含 4 个净取出的月份，要显示成负柱不能藏起来），
  计划是 **400,000 円/月**，目标 **100,000,000 円**，预期年化 **7%**。

组合大致构成：指数基金（楽天プラスS&P500 / オルカン / FANG+ / NASDAQ-100 等）、
12 只左右美股个股、3 只日本股/REIT、金现物 72.25g + GXゴールド、米ドルMMF、預り金、BTC。
账户维度：NISAつみたて枠 / NISA成長投資枠 / 特定口座 / 一般口座。

---

## 7. 已经验证有效的设计取向（建议沿用）

- **存数量，不存市值。** 数量只在买卖时变，市值天天变、手填必然过期。
  评价额 = 数量 ÷ 倍率 × 价格 × 汇率。这一步把每月要填的数字从 30 个降到 6 个。
- **实时价取不到就退回手填价**，让 GOOGLEFINANCE 不可用时表格降级而不是崩掉。
- 同一支基金跨多个账户时，后面的行引用第一行的价格，基準価額只填一次。
- 手填的东西越少越好，能从 CSV 导入的一律走 CSV，键用楽天自己的写法 `(銘柄, 口座)`。
- **不要出现 bitFlyer 的 API secret**（也确实不需要 —— 数量已知，价格走 GOOGLEFINANCE）。
  密钥不该出现在聊天记录或代码库里。
- 日常只有两件事需要用户动手：季度更新持株会/ゆうちょ余额；用现金单独买股票时补一笔投入。

---

## 8. 一个流程上的问题，重做时要顺手解决

`CLAUDE.md` 里的 §6/§7 要求有 `PROGRESS.md`、`docs/decisions.md`、`docs/specs/`、
`docs/sdd-workflow.md` 和 SDD 提案流程 —— **这些文件一个都不存在**，而且 `CLAUDE.md`
本身是未跟踪的。也就是说规则被放进来了但整个分支从没执行过。

开新分支时二选一：要么把 docs 骨架建起来，要么把 §6/§7 改成实际会遵守的样子。
现在这个状态最糟 —— 规则描述了一个不存在的结构。

---

## 9. 还没搞清楚的事

「Gmail 版报的数字用户不信任」到底是**数字确实算错了**，还是**没法追溯所以不敢信**？
这两种情况指向完全不同的解法（前者要修解析，后者要做可追溯性/让用户能对账）。
重做之前应该先问清楚这一点。
